const reportWebVitals = (onPerfEntry) => {
  // Standard Create React App helper.
  // Kept as a placeholder to ensure the build process does not fail.
  if (onPerfEntry && typeof onPerfEntry === 'function') {
    onPerfEntry({});
  }
};

export default reportWebVitals;